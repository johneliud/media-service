name: Ping Service

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping media-service
        run: curl -s -o /dev/null -w "%{http_code}" https://media-service-1vmx.onrender.com || true
